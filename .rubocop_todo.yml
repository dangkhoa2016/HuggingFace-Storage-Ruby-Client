# This configuration was generated by
# `rubocop --auto-gen-config`
# on 2026-06-24 04:41:02 UTC using RuboCop version 1.87.0.
# The point is for the user to remove these configuration records
# one by one as the offenses are removed from the code base.
# Note that changes in the inspected code, or installation of new
# versions of RuboCop, may require this file to be generated again.

# ── Bundler ──────────────────────────────────────────────────────────────
#   Development dependencies use gemspec style by design (preferred for local dev).
Gemspec/DevelopmentDependencies:
  Enabled: false

# ── Metrics ──────────────────────────────────────────────────────────────
#   XetShardBuilder is a focused module for binary shard protocol assembly.
#   Splitting further would harm cohesion — all methods belong together.
Metrics/ModuleLength:
  Max: 150

# ── Linting ──────────────────────────────────────────────────────────────
#   Thor DSL requires method stubs with desc/option/long_desc annotations
#   that delegate via super() to included modules. Not useless — required.
Lint/UselessMethodDefinition:
  Exclude:
    - "src/hugging_face_storage/cli.rb"

# ── RSpec ────────────────────────────────────────────────────────────────
RSpec/ExampleLength:
  Exclude:
    - "spec/hugging_face_storage/api_client_spec.rb"

# ── Style ────────────────────────────────────────────────────────────────
#   Spec files using single quotes for JSON body strings (contain internal double quotes).
Style/StringLiterals:
  Exclude:
    - "spec/hugging_face_storage/api_client_spec.rb"
    - "spec/hugging_face_storage/logger_spec.rb"

